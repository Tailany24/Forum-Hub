spring.application.name=hub

# Configura��es do Banco de Dados
spring.datasource.url=jdbc:mysql://localhost:3306/hanami?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=T@i96681075

# Porta do Servidor (defina uma porta espec�fica ou deixe como 0 para porta aleat�ria)
server.port=8080

# Configura��es do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=true
spring.jpa.defer-datasource-initialization=true

# Inicializa��o do SQL
spring.sql.init.mode=always

# Configura��es do Thymeleaf
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Configura��es do MVC
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

